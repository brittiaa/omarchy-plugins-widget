node_modules/
package-lock.json
*.log
.zcode/

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/

# Per-developer AI assistant configuration. These are set up however whoever is
# working here likes them, they are regenerated by the tools that own them, and
# none of them is part of the plugin. CLAUDE.md is deliberately not here: that
# one is documentation of this repo, written by hand and reviewed like any
# other file.
.claude/
.qodo/
.mcp.json
.ignore
